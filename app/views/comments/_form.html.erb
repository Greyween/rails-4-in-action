<h2>New Comment</h2>
<%= form_for [@ticket, @comment] do |f| %>
	<ul>
	<%= @comment.errors.full_messages.each do |msg| %>
		<li><%= msg %></li>
	<% end %>
	</ul>	
	<p>
		<%= f.label :text %><br/>
		<%= f.text_field :text %>
	</p>
	<%= f.submit %>
<% end %>